function Header({children}) {
    return (
        <div className="py-3">
            <h1 className="font-bold text-black text-lg text-center">{children}</h1>
        </div>
    )
}

export default Header
